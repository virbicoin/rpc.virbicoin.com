# Server port (default: 3000)
PORT=4000

# Node configuration (JSON format: {"Display Name": "RPC URL", ...})
# Required for the dashboard to display node statuses.
# NODES='{"Node 1":"http://host1:8329","Node 2":"http://host2:8329"}'
